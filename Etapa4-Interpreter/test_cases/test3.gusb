|[
    declare
        x, y, z, p : int, int, array[0..2], array[0..2]

    x := 1;
    //y := 1;
    //print y;
    z:= 4, 3, 2;
    println z;
    for x in 1 to 5 -->
        |[
            for q in 1 to 5 -->
                |[
                    declare
                        w : bool
                ]|
            rof
        ]|
    rof;
    //print w;
    //w := true;
    x := 7
]|